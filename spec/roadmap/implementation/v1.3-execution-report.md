# Version v1.3 — Execution Report

**Date:** 2026-07-01
**Branch:** v1.1-dev
**Label:** v1::version:1 (phase v1.3)
**Target version:** 1.3.0 (not yet released — see Next Steps)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 6 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All six v1.3 issues (KILN-061…066) are implemented, validated, committed, pushed, and closed. The
loop is now an explicit, table-driven finite state machine; Agnika is **byte-for-byte** (the whole
suite green + the deterministic dry-run unchanged). Test suite grew **381 → 452**.

## Issues

| # | KILN ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | KILN-061 | State + event kinds + transition table + `advance` (gate) | v1.3 | completed | `40e4e21` | 2 | pass (+19) |
| 2 | KILN-062 | Event queue + producers (fold inbox + rotate_results) | v1.3 | completed | `88b3c26` | 2 | pass (+18) |
| 3 | KILN-063 | Action-registry seam (built-ins as tools) | v1.3 | completed | `4e126ea` | 3 | pass (+18) |
| 4 | KILN-064 | Rewire `run()` as a thin FSM driver (behaviour-preserving) | v1.3 | completed | `cd8b3c0` | 7 | pass (+4) |
| 5 | KILN-065 | `cooling` as a real state | v1.3 | completed | `c162645` | 7 | pass (+5) |
| 6 | KILN-066 | Transition tracing + FSM contract + ARCHITECTURE | v1.3 | completed | `91a8a0c` | 4 | pass (+7) |

(A separate docs commit `42fc69e` landed the how-it-works v1.3 rewrite + UK translation, the new
`docs/state-machine.md`, and the EFSM framing in `spec/features/fsm.md` / ROADMAP §1.7.)

## Detailed Results

### KILN-061: State + event kinds + transition table + advance
**Status:** completed · **Commit:** `40e4e21`
New leaf module `kiln/fsm.py` — `State` enum (idle/thinking/responding/cooling/resting), `EventKind`
vocabulary (+ reserved peer/room/tool.result), frozen `Event`/`Ctx`/`Rule`, `DEFAULT_TABLE` encoding
v1.2, pure `advance`. Self-triggers route by the configured `reach_out_need`/`reflect_need`, not a
hardcoded string.
**Validation:** [x] 19 unit tests · [x] full suite (381→400) · [x] dry-run · [x] ruff · [x] acceptance

### KILN-062: Event queue + producers
**Status:** completed · **Commit:** `88b3c26`
`EventQueue.drain_one` pops one event/tick by `EVENT_PRIORITY` (input > self-trigger > rotate > tick);
pure producers (`input_event`/`self_trigger_event`/`rotate_event`/`tick_event` + `gather_events`).
**Validation:** [x] 18 unit tests · [x] full suite (400→418) · [x] dry-run · [x] ruff · [x] acceptance

### KILN-063: Action-registry seam
**Status:** completed · **Commit:** `4e126ea`
`ActionRegistry` (`name → callable`, `fire(action, ctx)`) + `ActionContext` + built-ins wrapping the
v1.2 branches; `default_registry()` covers `fsm.table_actions()` exactly. ARCHITECTURE §Contracts
pins the seam.
**Validation:** [x] contract + 18 unit tests · [x] full suite (418→436) · [x] dry-run · [x] ruff · [x] acceptance

### KILN-064: Rewire run() as a thin FSM driver
**Status:** completed · **Commit:** `cd8b3c0`
`run()` now drives the FSM: gather events → `drain_one` → `advance` → `registry.fire` → read back the
outcome. Drift/satiation/persistence/rotation/the rest gate untouched (rest kept as a flag; the
resting-tick fall-through fires `enter_rest`). ARCHITECTURE §The tick loop promoted.
**Validation:** [x] **dry-run byte-for-byte** · [x] whole suite unchanged (436→440, +4 integration) · [x] ruff · [x] acceptance

### KILN-065: cooling as a real state
**Status:** completed · **Commit:** `c162645`
A turn → COOLING; `cooling + tick` runs `cool` (idle satiation) while cooldowns are active, then
`idle`. Driver carries `fsm_state` + passes cooldowns into `Ctx`. ARCHITECTURE §Status event pins the
state set (keys unchanged; value gains `cooling`).
**Validation:** [x] integration + snapshot-keys contract + 3 unit · [x] full suite (440→445) · [x] byte-for-byte output · [x] ruff · [x] acceptance

### KILN-066: Transition tracing + FSM contract + ARCHITECTURE
**Status:** completed · **Commit:** `91a8a0c`
`fsm.trace_record`/`TRACE_KEYS`/`guard_name`/`match`; `engine.run(trace=…)` optional sink (off by
default). ARCHITECTURE §Contracts pins the full FSM seam; the WS event-protocol tests stay green.
**Validation:** [x] 7 tests (shape, on/off, guard naming, advance/match) · [x] full suite (445→452) · [x] WS protocol green · [x] ruff · [x] acceptance

## Phase DoD (ROADMAP §v1.3) — met

- [x] the loop is an FSM over a queued transition table
- [x] `advance(state, event)` is unit-tested
- [x] the default table reproduces v1.2 behaviour (Agnika byte-for-byte, all tests green)
- [x] `cooling` is a real state
- [x] every transition is traced
- [x] the server emits the same events

## Next Steps

- **Release:** `/release-version 1.3.0` — the phase is complete, so a true 1.3.0 (the FSM actually
  drives the loop) is now warranted.
- **Then v1.4** (RAG) per the roadmap; the deferred `engine.py` split (into
  `needs`/`routing`/`channels`/`actions`) is queued for a following version.
- **Later phases build on this FSM:** 1.7 declarative `fsm.yaml` (the EFSM — needs as machine
  variables), 1.8 simulation (consumes the KILN-066 trace), 1.9 non-blocking execution.
