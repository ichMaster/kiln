# Version v1.2 — Execution Report

**Date:** 2026-06-30
**Branch:** v1.1-dev
**Label:** v1::version:1
**Target version:** 1.2.0 (not bumped — awaiting `/release-version`)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 5 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All five v1.2 issues (KILN-056…060) are implemented, validated, committed, pushed, and closed. The
test suite grew from **364** (v1.1 baseline) to **379** (+15 new), all green; ruff + dry-run clean
throughout; **zero paid calls** (MockBrain + stubbed model helpers). Agnika is **byte-for-byte
unchanged** — the back-compat contract held at every step.

## Issues

| # | KILN ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | KILN-056 | AgentConfig + per-agent loaders | v1.2 | completed | 1828bf8 | 3 | pass |
| 2 | KILN-057 | Thread AgentConfig through engine.run | v1.2 | completed | 6dff3c8 | 5 | pass |
| 3 | KILN-058 | Author Pashu's data (state/pashu/) | v1.2 | completed | a9b61e9 | 6 | pass |
| 4 | KILN-059 | Multi-agent host + permission scope | v1.2 | completed | 85b0e9b | 7 | pass |
| 5 | KILN-060 | Isolation e2e + routes/client + ARCHITECTURE | v1.2 | completed | 4be4b25 | 4 | pass |

Plus two chore commits: `28bc709` (baseline E501 fix before starting) and `aed7009` (ruff-format a
KILN-057 test).

## Detailed Results

### KILN-056: AgentConfig + per-agent loaders
**Status:** completed · **Commit:** 1828bf8
A frozen `config.AgentConfig` bundling one agent's full calibration (need model + tunables + mood);
`for_agent(id)` resolves each from `state/{id}/` via `AgentPaths`. The default agent reproduces the
module globals byte-for-byte; missing files heal to `DEFAULT_*`; dicts deep-copied (no aliasing).
Settled the open decision: `config.yaml` is per-agent. Engine untouched (additive gate).
**Validation:** [x] tests (4) · [x] ruff · [x] dry-run · [x] acceptance

### KILN-057: Thread AgentConfig through engine.run
**Status:** completed · **Commit:** 6dff3c8
`run(config=…)` threaded through `drift` / `apply_satiation` / the trigger selectors / `classify` /
`respond` / `_status_snapshot`, the loop scalars, and `mood_block`/`biorhythm` (optional `mood_cfg`).
Each helper takes an optional `config=None` reading the module global at call time — so direct callers
and tests monkeypatching `eng.TICK_SECONDS`/`THINK_THRESHOLD`/`REST_WAKE` still win, and agnika is
byte-for-byte. **Contract test:** two configs diverge (drift/routing/status); the default path is
unchanged; brain pick follows `config.chat_model`/`deep_model`.
**Validation:** [x] tests (4) · [x] ruff · [x] dry-run · [x] acceptance

### KILN-058: Author Pashu's data
**Status:** completed · **Commit:** a9b61e9
`state/pashu/{canon.md, needs_model.yaml, mood.json, prompts.md, config.yaml}` — a calm, grounded
companion (contrast to Agnika's fire), observably distinct (connection threshold 0.90 vs 0.80,
self_cooldown 8 vs 5, slower drift, think_threshold 0.55, agent_name "Pashu", own mood labels/cues).
`.kiln/pashu/` covered by the existing `.kiln/` gitignore.
**Validation:** [x] tests (2) · [x] ruff · [x] dry-run · [x] acceptance

### KILN-059: Multi-agent host + permission scope
**Status:** completed · **Commit:** 85b0e9b
`server.yaml` `agents: [agnika, pashu]` → `SERVER_AGENTS`; `host.boot_configured` starts each on its
own thread with its own `AgentConfig` + `AgentPaths` + scope. The default agent runs `config=None`
(globals, monkeypatch-safe); a companion gets its `AgentConfig`. Per-agent permission scope
(`agent_scope`: agnika broad, pashu narrow; set, **not enforced** until 1.5) in `GET /agents`.
**Validation:** [x] tests (4) · [x] ruff · [x] dry-run · [x] acceptance

### KILN-060: Isolation e2e + routes/client + ARCHITECTURE promotion
**Status:** completed · **Commit:** 4be4b25
End-to-end: host boots both agents, a TUI (WS) attaches to pashu and holds a turn, a turn on one agent
never touches the other's store/needs/config, `GET /agents` lists both with scopes, isolated
persistence. Stable across runs. ARCHITECTURE §Data model promoted (per-`agent_id` scoping current for
persistence + calibration); README/CLAUDE note the `agents:` list + `./connect.sh pashu`.
**Validation:** [x] tests (1 e2e) · [x] ruff · [x] dry-run · [x] acceptance

## Phase DoD (ROADMAP §v1.2)

> the host runs **Agnika and Pashu concurrently with isolated state** — a turn or self-trigger on one
> **never** touches the other's needs/store; a TUI attaches over WS to Pashu and holds a turn;
> `agent_id` isolation is contract-tested; all tests on `MockBrain` (zero paid calls).

**Every clause is demonstrated by a test** (KILN-059 concurrency + KILN-060 e2e). DoD met.

## Next Steps

- The phase is complete and unreleased. Cut it with `/release-version 1.2.0` when ready (it bumps the
  version metadata only — no source changes).
- Then the next phase: **v1.3 — State machine (FSM, events, queue)** → `/generate-issues 1.3`.
- A known follow-up (out of v1.2 scope): the `AGENT_NAME` transcript label and `load_birth` still read
  the module globals / per-agent canon, not `AgentConfig.agent_name`/`agent_birth` — cosmetic
  (persona-display in history.py/memory.py), behaviour is fully per-agent. Worth a small pass later.
