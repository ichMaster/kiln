# Version v0.2 — Execution Report

**Date:** 2026-06-25
**Branch:** v0.2-dev
**Label:** v0::version:0 (phase v0.2)
**Target version:** v0.2 → semver `0.2.0` (not yet released — see Next Steps)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 6 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All six issues for phase **v0.2 — Professional structure & tests (refactor)** are
implemented, validated, committed, pushed, and closed on GitHub. CI is green on
`v0.2-dev` (Python 3.10 + 3.12).

## Issues

| # | KILN ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | KILN-001 | Package layout & pyproject.toml (gate) | v0.2 | completed | eae9645 | 13 | pass (3) |
| 2 | KILN-002 | Brain seam — mockable Brain interface | v0.2 | completed | ff4523d | 6 | pass (13) |
| 3 | KILN-006 | Output seam toward the TUI bus | v0.2 | completed | d478fd4 | 4 | pass (16) |
| 4 | KILN-003 | Test suite (pytest, mock brain) | v0.2 | completed | 6e5076f | 5 | pass (55) |
| 5 | KILN-004 | Lint / format / types (ruff + mypy) | v0.2 | completed | 4669f41 | 15 | pass (55) |
| 6 | KILN-005 | CI workflow (ruff + pytest on push/PR) | v0.2 | completed | 6908bca | 2 | CI green |

*(Executed in dependency order: 001 → 002 → 006 → 003 → 004 → 005. The "Tests"
column shows the cumulative passing count after each issue.)*

## Detailed Results

### KILN-001: Package layout & pyproject.toml (gate)
**Status:** completed · **Commit:** eae9645 · **GitHub:** #1
**What:** Moved the six flat modules into a `kiln/` package (relative imports, DAG
intact); added `kiln/__main__.py` (`main()`) and moved the dry-run demo out of
`engine.py`; replaced `requirements.txt` with `pyproject.toml` (anthropic runtime;
pytest/ruff dev; console entry `kiln = kiln.__main__:main`; dynamic version from
`VERSION`); state/history resolve to the project root (or `KILN_HOME`).
**Validation:** [x] `pip install -e .` builds `kiln-0.1.0` · [x] `kiln` + `python -m
kiln` exit 0 · [x] pytest (needs.json round-trip contract) · [x] py_compile/import

### KILN-002: Brain seam — mockable Brain interface
**Status:** completed · **Commit:** ff4523d · **GitHub:** #2
**What:** Extracted `chat_reply`/`deep_reply` into `kiln/brain.py` as `LiveBrain`
behind a `runtime_checkable` `Brain` Protocol; added `MockBrain`; `respond()`/`run()`
route through an injected brain returning `(text, usage)`; replaced the
`log_model`/`take_usage` global with the pure `usage.usage_record()`. `engine.py`
imports neither `anthropic` nor `subprocess`.
**Validation:** [x] pytest 13 · [x] dry-run shows mock token line · [x] grep proves
core purity · [x] ARCHITECTURE + brain-seam contract test

### KILN-006: Output seam toward the TUI bus
**Status:** completed · **Commit:** d478fd4 · **GitHub:** #6
**What:** Added `kiln/output.py` (`Output` Protocol + `ConsoleOutput`); `run()`
writes every reply/usage/notice through an injected output port — no direct `print`.
Foreshadows the v1.1/1.2 event protocol.
**Validation:** [x] pytest 16 · [x] grep: no `print(` in engine.py · [x] dry-run
output byte-identical · [x] ARCHITECTURE + output-seam contract test

### KILN-003: Test suite (pytest, mock brain)
**Status:** completed · **Commit:** 6e5076f · **GitHub:** #3
**What:** Added unit coverage (drift/satiation incl. catch-up, classify/turn_weight,
self-trigger hysteresis+cooldown, `_usage_tokens`, `_cli_error_detail`) plus the
`respond()` return-contract + full-turn integration. 55 tests, all on `MockBrain`.
**Validation:** [x] `pytest` 55 passed · [x] green with `ANTHROPIC_API_KEY` unset ·
[x] no writes to real state/history

### KILN-004: Lint / format / types (ruff + mypy config)
**Status:** completed · **Commit:** 4669f41 · **GitHub:** #4
**What:** Added `[tool.ruff]` (E/W/F/I/UP/B, line-length 100) + advisory
`[tool.mypy]`; ran `ruff format` across package + tests; fixed the E501 findings.
**Validation:** [x] `ruff check` clean · [x] `ruff format --check` clean · [x]
pytest 55 still green · [x] smoke unchanged

### KILN-005: CI workflow (ruff + pytest on push/PR)
**Status:** completed · **Commit:** 6908bca · **GitHub:** #5
**What:** `.github/workflows/ci.yml` — push + PR, matrix py3.10/3.12, `pip install
-e .[dev]`, ruff check + ruff format --check + pytest + dry-run smoke; no API key.
Pinned `ruff>=0.15,<0.16` for deterministic formatting.
**Validation:** [x] CI ran on push to `v0.2-dev` and is **green** on 3.10 + 3.12 ·
[x] no `ANTHROPIC_API_KEY` in the job (zero paid calls)

## Phase DoD (ROADMAP §v0.2) — all met

- [x] `pip install -e .` installs the `kiln` package and the `kiln` command runs
- [x] `pytest` is green against a mock brain (**zero paid calls**; green with no API key)
- [x] `ruff check` is clean
- [x] CI runs on push (green on 3.10 + 3.12)
- [x] the core depends only on the brain seam (`engine.py` imports no `anthropic`/`subprocess`)

**Contracts pinned this phase:** the brain seam (`Brain → (text, usage)`), the output
seam, `needs.json` shape, `respond()` return `{class, route, reply, usage}`, and the
usage dict `{model, input, output, total}` — each with a contract test.

## Next Steps

- **No version bump performed.** All v0.2 issues are done, so the phase is ready to
  cut as semver `0.2.0` via `/release-version 0.2` — left to the user's discretion.
- **Merge `v0.2-dev` → `main`** when ready (open a PR; CI will gate it).
- Non-blocking: bump `actions/checkout@v4` / `setup-python@v5` to silence the Node 20
  deprecation annotation.
- Next phase: **v0.3 — Implement TUI** (Textual app + the echo-free bus that plugs
  into the v0.2 `Output` seam). Decompose with `/generate-issues 0.3`.
