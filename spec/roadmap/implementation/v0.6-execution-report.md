# Version v0 — Execution Report (phase v0.6)

**Date:** 2026-06-27
**Branch:** v0.2-dev
**Label:** v0::version:0
**Target version:** 0.6.0 (phase v0.6 — Long memory, user facts)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All v0.6 issues implemented, tested, committed, pushed, and closed. Test suite 160 → **181**
(all on a mock brain / mocked subprocess — zero paid calls); `ruff` clean; dry-run smoke green
throughout.

## Issues

| # | KILN ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | KILN-022 | `facts` section in the store + dedupe helper | v0.6 | completed | `615dc59` | 3 | pass |
| 2 | KILN-023 | Extract user facts on session close (Opus + thinking) | v0.6 | completed | `5b6bee0` | 5 | pass |
| 3 | KILN-024 | Digest all facts to N lines on start (Opus + thinking) | v0.6 | completed | `b97766c` | 5 | pass |
| 4 | KILN-025 | Facts → system prompt (`## Facts about the user`) | v0.6 | completed | `258c94d` | 8 | pass |

## Detailed Results

### KILN-022: `facts` section in the store + dedupe helper
**Status:** completed · **Commit:** `615dc59` (closes #22)
`empty_store()` gains a fourth section `facts`; `load_store` heals a missing key (v0.5 stores load
forward). `add_facts(store, texts, session_id, stamp)` appends `{id, text, first_seen, last_seen,
source_session}` deduped by normalized text (repeat → `last_seen` bump, origin kept). ARCHITECTURE
§Memory + §Contracts (Store seam).
**Validation:** [x] tests · [x] ruff · [x] dry-run (`facts: []`) · [x] acceptance

### KILN-023: Extract user facts on session close
**Status:** completed · **Commit:** `5b6bee0` (closes #23)
`memory.extract_facts(history, existing_facts, live)` via `claude -p` on `DEEP_MODEL` + `claude_env()`
(thinking on, API key stripped); shown existing facts, asked for only new ones; `_parse_facts`
(JSON array / fence / line fallback). `run()` close folds them in via `add_facts` after the
summary. Dry-run / CLI error → `[]`.
**Validation:** [x] tests (Opus/thinking/key-stripped + run()-close dedupe integration) · [x] ruff · [x] dry-run · [x] acceptance

### KILN-024: Digest all facts to N lines on start
**Status:** completed · **Commit:** `b97766c` (closes #24)
`config.FACTS_DIGEST_LINES` (default 8, `.env`-overridable). `memory.digest_facts(live)` condenses
all stored facts via `claude -p` on `DEEP_MODEL` + `claude_env()` to ≤ N lines (cap enforced
defensively); read-only. No facts / dry-run / CLI error → `""`.
**Validation:** [x] tests (Opus/thinking/key-stripped + line cap + config-driven) · [x] ruff · [x] `.env` override · [x] acceptance

### KILN-025: Facts → system prompt
**Status:** completed · **Commit:** `258c94d` (closes #25)
`build_system(canon, memory, facts="")` appends a dedicated `## Facts about the user` section after
canon + the v0.5 memory block; `facts=""` is v0.5 byte-for-byte. `run()` start computes
`digest_facts(live)` once and passes it in. ARCHITECTURE §Contracts (system-prompt composition
seam). The run()-invoking test helpers isolate the new start-time digest call.
**Validation:** [x] tests (contract + back-compat + run()-start integration) · [x] ruff · [x] dry-run · [x] acceptance

## Phase DoD (ROADMAP §0.6) — met

- [x] Closing a session extracts durable user facts (Opus + thinking) into `.kiln/store.json`.
- [x] Starting a session digests all facts to N lines (Opus + thinking).
- [x] The digest is injected as a dedicated `## Facts about the user` system-prompt section,
      separate from the canon and the v0.5 memory summaries.
- [x] Both Opus calls run via `claude -p` with the API key stripped (subscription) — asserted on
      the captured subprocess env.

## Cost / auth note

Facts extraction (KILN-023) and the digest (KILN-024) run on **Opus** via `claude -p` with
`claude_env()` (extended thinking on, `ANTHROPIC_API_KEY` stripped) — the durable layer warrants
Opus, deliberately distinct from v0.5's now-Haiku session summary. Tests mock the subprocess and
assert the key-stripping invariant; the dry-run paths are stubs. Zero paid calls in CI.

## Next Steps

- **Release:** phase v0.6 → semver `0.6.0` — run `/release-version 0.6.0` when ready (the skill
  does not bump the version automatically).
- **Next phase:** v0.7 (Tokens & cost report — Lumi-style).
- Companion: `v0.6-issues.md` (source), `v0.6-github-report.md` (KILN → #22–25).
