# Version v0 — Execution Report (phase v0.5)

**Date:** 2026-06-27
**Branch:** v0.2-dev
**Label:** v0::version:0
**Target version:** 0.5.0 (phase v0.5 — Short memory, `.kiln/store.json`)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 5 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All v0.5 issues implemented, tested, committed, pushed, and closed. Test suite 136 → **157**
(all on the mock brain — zero paid calls); `ruff` clean; dry-run smoke green throughout.

## Issues

| # | KILN ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | KILN-017 | Store module + `.kiln/store.json` (schema, atomic write, `.bak`) | v0.5 | completed | `5223c4d` | 4 | pass |
| 2 | KILN-018 | Session close writes sessions + messages + summary to the store | v0.5 | completed | `37ba11f` | 9 | pass |
| 3 | KILN-019 | Review & prune the session before storing | v0.5 | completed | `f83a552` | 4 | pass |
| 4 | KILN-020 | Summaries → system prompt from the store | v0.5 | completed | `f293a72` | 2 | pass |
| 5 | KILN-021 | Migrate legacy data + in-flight conversation into the store | v0.5 | completed | `faa223e` | 3 | pass |

## Detailed Results

### KILN-017: Store module + `.kiln/store.json`
**Status:** completed · **Commit:** `5223c4d` (closes #17)
New `kiln/store.py` (`load_store`/`save_store`, atomic write + `.bak`, corrupt-recovery, heal),
`KILN_DIR`/`STORE_FILE` config, ARCHITECTURE §Memory + §Contracts. 7 contract/unit tests.
**Validation:** [x] tests · [x] ruff · [x] dry-run · [x] acceptance

### KILN-018: Session close writes to the store
**Status:** completed · **Commit:** `37ba11f` (closes #18)
`run()` finally writes `sessions`/`messages` (before the summary) + one `summaries` entry into
the store; retired `save_session`/`save_summary`; `.kiln/` gitignored; 5 run() test helpers
re-isolated to the store; close→store integration test.
**Validation:** [x] tests · [x] ruff · [x] dry-run (writes `.kiln/store.json`) · [x] acceptance

### KILN-019: Review & prune before storing
**Status:** completed · **Commit:** `f83a552` (closes #19)
`memory.prune_history` (empty / slash-echo / resting-notice); the close path skips all-noise
sessions. 4 prune unit tests + a noise-only-not-stored integration test.
**Validation:** [x] tests · [x] ruff · [x] dry-run · [x] acceptance

### KILN-020: Summaries → system prompt from the store
**Status:** completed · **Commit:** `f293a72` (closes #20)
`load_memory()` reads the store's `summaries` (oldest→newest, `## Conversation` blocks) instead
of `memory.md`; `build_system` unchanged. 5 contract/unit tests + an end-to-end check.
**Validation:** [x] tests · [x] ruff · [x] acceptance

### KILN-021: Migrate legacy data into the store
**Status:** completed · **Commit:** `faa223e` (closes #21)
`memory.migrate_legacy()` folds `state/memory.md` + `history/session-*.json` into the store,
idempotently (guarded by store non-emptiness); called once at startup in `__main__`. 3 unit
tests; smoke migrated the repo's real legacy data (158 sessions, 33 summaries).
**Validation:** [x] tests · [x] ruff · [x] dry-run (migrates real legacy files) · [x] acceptance

## Phase DoD (ROADMAP §v0.5) — met

- [x] All session state lives in one `.kiln/store.json`.
- [x] The kept history is pruned to real conversation.
- [x] Legacy `state/memory.md` + `history/*.json` are migrated in (idempotently, on startup).
- [x] Closing a session writes one summary.
- [x] Every new session's system prompt carries all prior summaries.

## Next Steps

- **Release:** phase v0.5 → semver `0.5.0` — run `/release-version 0.5.0` when ready (the skill
  does not bump the version automatically).
- **Next phase:** v0.6 (Long memory — user facts) builds on this store (`facts` section).
- Companion: `v0.5-issues.md` (source), `v0.5-github-report.md` (KILN → #17–21).
