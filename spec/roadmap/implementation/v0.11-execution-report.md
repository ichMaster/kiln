# Version v0.11 — Execution Report

**Date:** 2026-06-29
**Branch:** v0.2-dev
**Label:** v0::version:0
**Target version:** 0.11.0
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | KILN ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | KILN-045 | Curiosity need + config (drift, threshold, clean registration) | v0.11 | completed | 18ebfe5 | 9 | pass |
| 2 | KILN-046 | `curiosity_nudge` provocation (pure mood.py + persona text) | v0.11 | completed | 13c2700 | 3 | pass |
| 3 | KILN-047 | Discharge on asking + curiosity reply marker | v0.11 | completed | 0d3f535 | 9 | pass |
| 4 | KILN-048 | Curiosity → system prompt (`build_system` layer + per-turn wiring) | v0.11 | completed | a27cd05 | 5 | pass |

## Detailed Results

### KILN-045: Curiosity need + config
**Status:** completed · **Commit:** 18ebfe5 · **Validation:** [x] unit · [x] dry-run · [x] ruff · [x] acceptance
`curiosity` in DRIFT (0.002) + CURIOSITY / CURIOSITY_THRESHOLD (0.5) / CURIOSITY_SATIATION (0.4); **not** a trigger. Registered in `state/mood.json` as nudge-only (`label «цікавість»`, `mood_line: false`) — `mood_block` skips `NEED_NOMOOD`, so it shows in the TUI panel but not in `## Настрій`. Seeded; the v0.10 `load_state` heal covers older files. ARCHITECTURE §Needs updated.

### KILN-046: curiosity_nudge provocation
**Status:** completed · **Commit:** 13c2700 · **Validation:** [x] unit · [x] ruff · [x] acceptance
Pure `curiosity_nudge(curiosity, threshold)` → the `## Цікавість` block at/above the threshold, `""` below. Text from `CURIOSITY_NUDGE_TEXT`, sourced from the optional `curiosity_nudge` field in `state/mood.json` (with a `DEFAULT_CURIOSITY_NUDGE` fallback).

### KILN-047: Discharge on asking + reply marker
**Status:** completed · **Commit:** 0d3f535 · **Validation:** [x] unit+integration · [x] contract · [x] dry-run · [x] ruff
Pure `is_curiosity_reply` (a `?` or a leading Ukrainian interrogative). `_turn` discharges curiosity by `CURIOSITY_SATIATION` when the nudge was active and the reply asks (a statement leaves it high). Output seam gains `is_curiosity` (Console «Agnika (?)» / TuiOutput bridge / TUI render). `respond()`'s `{class, route, reply, usage}` shape untouched. ARCHITECTURE §Output updated.

### KILN-048: Curiosity → system prompt
**Status:** completed · **Commit:** a27cd05 · **Validation:** [x] contract+unit · [x] dry-run · [x] ruff · [x] ARCHITECTURE
`build_system(..., curiosity="")` appends `## Цікавість` after `## Думки`; `curiosity=""` is byte-for-byte v0.10. `_system()` composes the nudge **per turn** (gated on `CURIOSITY` + the threshold). ARCHITECTURE §Canon/system-prompt updated.

## Phase DoD — met

A `curiosity` («цікавість») need with a **low threshold (~0.5)** and a **very small drift** (0.002) exists; while it is over the threshold, **every message's** system prompt carries a `## Цікавість` provocation to **ask questions and dig deeper rather than mirror**; when she **actually asks** (a detected, marked curiosity reply) curiosity is **discharged** by `CURIOSITY_SATIATION`, so it **ebbs and flows** (curious → asks → sated → curious); it's a **per-turn prompt nudge** with **no separate self-message** (`curiosity` is not in `NEED_TRIGGERS`); `CURIOSITY` / `CURIOSITY_THRESHOLD` / `CURIOSITY_SATIATION` are configurable; deterministic; local — no external calls.

**Test suite:** 308 → **321 passed** (+13 across the four issues), zero paid calls. ruff clean. Dry-run smoke clean.

**Contracts pinned this phase:**
- **Output seam** — `Output.agent(..., is_curiosity)` (KILN-047) + contract test.
- **Canon / system prompt** — `build_system(..., curiosity)` (KILN-048) + contract test.

## Next Steps

- v0.11 is complete — all four issues (#45–#48) closed.
- New `.env` knobs: `CURIOSITY`, `CURIOSITY_THRESHOLD`, `CURIOSITY_SATIATION`.
- Release when ready: `/release-version 0.11.0`.
- v0.11 was the last planned **v0** phase. The next planned work is **v1 — Engine (the tick-server & hub foundation)**.
