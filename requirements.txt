# kiln — залежності.
#
# Потрібні ЛИШЕ для живої гілки чату (chat_reply -> Anthropic Messages API).
# Dry-run і жива гілка роздумів/тулів (через `claude` CLI) працюють на самій
# стандартній бібліотеці — без жодних пакетів.
anthropic>=0.111   # перевірено на 0.111.0 / Python 3.14
